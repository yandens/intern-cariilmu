const createCourse = require("./create");
const readCourse = require("./read");
const updateCourse = require("./update");
const deleteCourse = require("./delete");

module.exports = { createCourse, readCourse, updateCourse, deleteCourse };
